import { readFile, stat } from 'node:fs/promises';

const files = '../downloads/m3gim-fulltext/';
const packageRoot = new URL('../downloads/m3gim-fulltext/', import.meta.url);
const manifest = JSON.parse(await readFile(new URL('manifest.json', packageRoot), 'utf8'));
const prompt = await readFile(new URL('prompt.txt', packageRoot), 'utf8');
const pngSize = ((await stat(new URL('m3gim-png.zip', packageRoot))).size / 1e6).toFixed(1);
const escape = value => String(value).replaceAll('&', '&amp;').replaceAll('<', '&lt;').replaceAll('>', '&gt;');

export function m3gimCard(base, link) {
  return `<div class="panel"><p class="eyebrow">Session 2 · Hands-on</p><h3>Build the M³GIM Full-Text Corpus</h3><p>Transcribe seven facsimiles with an LLM. Start with a two-page programme, revise your prompt, then complete all 40 scan pages.</p><div class="actions">${link(base + 'materials/m3gim-fulltext.html', 'Open the exercise & downloads', 'button')}</div><p class="small">PDFs, prepared PNGs, a conversion script, prompts and edited reference texts.</p></div>`;
}

export function m3gimPage(link) {
  const download = (file, label, style = 'button secondary') => link(files + file, label, style).replace('<a', '<a download');
  const documents = manifest.documents.map(doc => `<li><strong>${doc.id}</strong> · ${doc.scan_pages} scan pages<br>${download(doc.pdf, 'Source PDF', '')} · ${link(doc.source_url, 'Source on Google Drive')}</li>`).join('');
  const texts = manifest.documents.map(doc => `<li>${download('text/' + doc.id + '.txt', doc.id + ' · full text', '')} <span class="small">(${doc.scan_pages} scans)</span></li>`).join('');
  return `<div class="hero session"><p class="eyebrow">Session 2 · After “Prompting Strategies”</p><h1>Build the M³GIM<br>Full-Text Corpus</h1><p class="lead">Use an LLM to turn seven facsimiles into complete transcriptions. Test your prompt on the first programme, then apply and review it across all 40 scan pages.</p><div class="actions">${link('#materials', 'Get the materials', 'button')}${link('#workflow', 'Follow the exercise', 'button secondary')}${link('#reference', 'Reference texts', 'button secondary')}</div></div>
<section class="panel"><h2>Your result</h2><p>Seven UTF-8 text files, one per document, together with your final prompt and a short correction log. Keep the original model output separately.</p><p class="small">Sources: Ira Malaniuk materials used in M³GIM (Mapping Mobile Musicians), archive of the University of Music and Performing Arts Graz (UAKUG), collection NIM.</p></section>
<section id="materials"><h2>1. Download the facsimiles</h2><p>The corpus contains five two-page documents, one six-page document and one 24-page booklet. Each PNG corresponds to one PDF scan page. Some scans contain two printed pages.</p><div class="actions">${download('m3gim-pdf.zip', 'All 7 PDFs · ZIP, 4.4 MB', 'button')}${download('m3gim-png.zip', 'All 40 PNGs · ZIP, ' + pngSize + ' MB', 'button secondary')}${download('m3gim-instructions.zip', 'Instructions, prompt & converter · ZIP')}</div><details><summary>Individual PDFs and source links</summary><ul class="resources">${documents}</ul></details></section>
<section><h2>2. Prepare the images</h2><p>Use the prepared PNGs directly, or convert the PDFs yourself. Download the converter, extract the PDFs into a folder called <code>pdf</code> beside it, then run:</p><pre><code>uv run pdf_to_png.py pdf png</code></pre><p>The converter creates one image per scan at 220 dpi. Filenames retain the source ID and page order, for example <code>UAKUG_NIM_005_137_3_p001.png</code>. Keep spreads intact.</p><div class="actions">${download('pdf_to_png.py', 'Download the converter')}${download('exercise.md', 'Full instructions · Markdown')}</div><details><summary>Using an existing Python installation</summary><pre><code>python -m pip install pypdfium2 Pillow
python pdf_to_png.py pdf png</code></pre><p>Run these commands from the folder containing the script and the <code>pdf</code> folder. The output folder must be new.</p></details></section>
<section id="workflow"><h2>3. Test, revise and complete the corpus</h2><ol class="workflow"><li><strong>Start with _3.</strong> Open a new conversation in an LLM interface that accepts images. Attach its two PNGs and use the start prompt below. Enter the document ID and image filenames.</li><li><strong>Inspect both pages.</strong> Check for omitted lines, changed names or spelling, and incorrect reading order. Save the first output, revise the prompt to address an observed problem, then compare the results.</li><li><strong>Continue with all six remaining documents.</strong> Use a new conversation for each. Begin with one or two images per request and adjust to page density and the interface’s output limit.</li><li><strong>Check every batch.</strong> Record scan numbers in the request. Verify that each page is complete; request remaining pages explicitly after an interrupted response. Merge the results in scan order.</li><li><strong>Edit against the facsimiles.</strong> Include covers, captions, advertisements and handwritten notes. Preserve source languages, spelling and uncertainty. Check table rows and empty cells.</li><li><strong>Save seven full texts.</strong> Name each file after its document ID. Record corrections with document, scan, original reading, corrected reading and image evidence.</li></ol><details open><summary>Start prompt · copy and adapt</summary><pre>${escape(prompt)}</pre><p>${download('prompt.txt', 'Download the prompt · TXT', '')}</p></details></section>
<section class="panel"><h2>Check the relationships in a table</h2><p>Open <strong>_11, scan 3: “Wer singt was?”</strong> Check whether each role remains attached to its performer and work. Then compare Ira Malaniuk’s entries in _10 and _11. Preserve what each source states.</p><p>A programme records an announced cast. Additional evidence is needed to establish who actually performed.</p></section>
<section id="reference"><h2>4. Compare with the reference texts</h2><p>Complete your own transcription pass, then use the reference to investigate differences. Resolve each difference against the image. Bring one corrected error and one unresolved reading to the discussion.</p><details><summary>Download the edited reference and review notes</summary><p>The reference covers all 40 scans. All page images were visually inspected for coverage and layout; selected readings were corrected and the cast table was reconstructed. Uncertain readings remain marked. Expert acceptance as a critical edition is pending.</p><div class="actions">${download('m3gim-reference.zip', 'All reference files · ZIP', 'button')}${download('review-notes.md', 'Review notes & correction log')}${download('UAKUG_NIM_005_137_11_p003.tsv', '“Wer singt was?” · TSV')}</div><p class="small">The ZIP includes raw model transcriptions, edited full texts, page-structured JSON, the table and provenance. Table footnotes remain in the full text.</p><ul class="resources">${texts}</ul></details></section>
<section><h2>Slide text for this exercise</h2><p>Four English slides with speaker notes, prepared for insertion after <em>Prompting Strategies: There Is No Prompt to Rule Them All</em>.</p><div class="actions">${download('slide-text.md', 'Download slide text · Markdown')}${download('manifest.json', 'Source and image manifest · JSON')}</div></section>
<div class="session-nav">${link('../sessions/session-2.html#exercises', '← Session 2')}${link('index.html', 'All materials →')}</div>`;
}
