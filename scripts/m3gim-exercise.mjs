import { readFile, stat } from 'node:fs/promises';

const files = '../downloads/m3gim-fulltext/';
const packageRoot = new URL('../downloads/m3gim-fulltext/', import.meta.url);
const read = name => readFile(new URL(name, packageRoot), 'utf8');
const manifest = JSON.parse(await read('manifest.json'));
const metadata = JSON.parse(await read('metadata.json'));
const prompts = await Promise.all(['prompt.txt', 'metadata-prompt.txt', 'tei-prompt.txt'].map(read));
const pngSize = ((await stat(new URL('m3gim-png.zip', packageRoot))).size / 1e6).toFixed(1);
const nextSize = ((await stat(new URL('m3gim-next-session.zip', packageRoot))).size / 1e6).toFixed(1);
const escape = value => String(value).replaceAll('&', '&amp;').replaceAll('<', '&lt;').replaceAll('>', '&gt;');

export function m3gimCard(base, link) {
  return `<div class="panel"><p class="eyebrow">Session 2 · Extended hands-on</p><h3>From Facsimiles to TEI</h3><p>Create complete transcriptions, extract document metadata and combine both in simple TEI-XML. Work through seven documents with 40 scan pages and keep the images linked for Session 3.</p><div class="actions">${link(base + 'materials/m3gim-fulltext.html', 'Open the exercise & downloads', 'button')}</div><p class="small">PDFs, PNGs, prompts, a TEI template, a local checker and complete reference files.</p></div>`;
}

export function m3gimNextSession(base, link) {
  return `<div class="panel"><h3>Continue with your TEI corpus</h3><p>Bring the seven TEI files and 40 PNGs prepared in Session 2. Keep the <code>tei/</code> and <code>png/</code> folders together so the image links remain usable.</p><div class="actions">${link(base + 'materials/m3gim-fulltext.html#next-session', 'Corpus layout & reference bundle', 'button secondary')}</div></div>`;
}

export function m3gimPage(link) {
  const download = (file, label, style = 'button secondary') => link(files + file, label, style).replace('<a', '<a download');
  const sources = manifest.documents.map(doc => `<li><strong>${doc.id}</strong> · ${doc.scan_pages} scans<br>${download(doc.pdf, 'Source PDF', '')} · ${link(doc.source_url, 'Source on Google Drive')}</li>`).join('');
  const references = manifest.documents.map(doc => `<li><strong>${doc.id}</strong> · ${doc.scan_pages} scans<br>${download('tei/' + doc.id + '.xml', 'TEI-XML', '')} · ${download('text/' + doc.id + '.txt', 'Full text · TXT', '')}</li>`).join('');
  const promptBox = (index, title, filename, open = false) => `<details${open ? ' open' : ''}><summary>${title}</summary><pre>${escape(prompts[index])}</pre><p>${download(filename, 'Download this prompt · TXT', '')}</p></details>`;
  const overview = metadata.documents.map(doc => `<tr><th scope="row">_${doc.id.split('_').at(-1)}</th><td>${escape(doc.title)}</td><td>${doc.languages.join(', ')}</td><td>${doc.date}<br><span class="small">${doc.date_type === 'publication' ? 'Publication' : 'Announced performance'}</span></td></tr>`).join('');
  return `<div class="hero session"><p class="eyebrow">Session 2 · After “Prompting Strategies”</p><h1>From Facsimiles<br>to TEI</h1><p class="lead">Combine the TEI you already know with an LLM workflow. Generate full texts from images, extract metadata and create seven TEI documents for the next session.</p><p class="workflow-line">Images → full text → metadata → TEI + images</p><div class="actions">${link('#materials', 'Get the materials', 'button')}${link('#workflow', 'Follow the exercise', 'button secondary')}${link('#reference', 'Reference solutions', 'button secondary')}</div></div>
<section class="panel"><h2>Your result</h2><p><strong>Seven TEI files linked to 40 PNG scans.</strong> Keep checked TXT files, raw model output, prompts and correction notes. Bring the complete folder to Session 3.</p><p class="small">Sources: Ira Malaniuk materials used in M³GIM (Mapping Mobile Musicians), archive of the University of Music and Performing Arts Graz (UAKUG), collection NIM.</p></section>
<section id="materials"><h2>1. Prepare the images</h2><p>The corpus contains five two-page documents, one six-page document and one 24-page booklet. Each PNG corresponds to one PDF scan; some scans contain two printed pages.</p><div class="actions">${download('m3gim-pdf.zip', 'All 7 PDFs · ZIP, 4.4 MB', 'button')}${download('m3gim-png.zip', 'All 40 PNGs · ZIP, ' + pngSize + ' MB')}${download('m3gim-instructions.zip', 'Instructions, prompts & TEI starter files · ZIP')}</div><p>Use the prepared PNGs, or convert the PDFs with the supplied script:</p><pre><code>uv run pdf_to_png.py pdf png</code></pre><p>Keep the document ID and three-digit scan number in each filename. The converter renders at 220 dpi and keeps spreads intact.</p><details><summary>Individual PDFs and source links</summary><ul class="resources">${sources}</ul></details><details><summary>Using an existing Python installation</summary><pre><code>python -m pip install pypdfium2 Pillow
python pdf_to_png.py pdf png</code></pre><p>Place the PDFs in a folder called <code>pdf</code> beside the script. The output folder must be new.</p></details></section>
<section id="workflow"><h2>2. Test the workflow on _3</h2><ol class="workflow"><li><strong>Transcribe both images.</strong> Attach the two PNGs to an image-capable LLM. Use the transcription prompt, inspect the output, then revise the prompt and correct the text.</li><li><strong>Extract and check metadata.</strong> Ask for the fields below with scan references and evidence. Keep unknown values explicit.</li><li><strong>Create one TEI file.</strong> Combine the checked text and metadata using the TEI template and prompt. Verify the XML and both image links.</li><li><strong>Apply the workflow to all seven documents.</strong> Use a new conversation for each. Process small image batches, check for missing pages and preserve the complete text.</li></ol>${promptBox(0, 'Transcription prompt · copy and adapt', 'prompt.txt')}</section>
<section id="metadata"><h2>3. Extract document metadata</h2><p>Record the <strong>title, languages, document type, date, place and source publisher where stated</strong>. Add the supplied identifier and archive context, plus the scan count. Give the source evidence for each value.</p><p>Distinguish an announced performance date from a publication date. Review all scans for languages, including inserts and short translated phrases. A composer named in a programme does not automatically become its author.</p>${promptBox(1, 'Metadata prompt · values, evidence and uncertainty', 'metadata-prompt.txt')}</section>
<section id="tei"><h2>4. Encode the result in simple TEI</h2><div class="grid"><article class="card"><h3>Metadata in the header</h3><p><code>teiHeader</code> holds the digital transcription description and the source description. <code>langUsage</code> records the languages present. Keep supplied context and extracted information distinguishable.</p></article><article class="card"><h3>Full text linked to images</h3><p><code>text/body</code> contains every scan’s text. Use <code>p</code> and <code>lb</code> for text structure, and <code>pb/@facs</code> to link each scan to its PNG.</p></article></div><pre><code>&lt;pb xml:id="scan_001" type="scan" n="1"
    facs="../png/UAKUG_NIM_005_137_3/UAKUG_NIM_005_137_3_p001.png"/&gt;</code></pre><p>Use one <code>pb</code> per PDF scan, including spreads. Keep handwriting and uncertainty markers as literal text in this introductory profile. For “Wer singt was?” in _11 scan 3, use <code>table/row/cell</code> and retain all five columns, including empty cells.</p><div class="actions">${download('tei-template.xml', 'TEI starter template')}${download('tei-guide.md', 'TEI field mapping & guide')}${download('exercise.md', 'Complete exercise · Markdown')}</div>${promptBox(2, 'TEI prompt · combine checked text and metadata', 'tei-prompt.txt')}</section>
<section><h2>5. Check the corpus</h2><p>Save seven complete XML files in <code>tei/</code>, alongside the <code>png/</code> directory. Copy the checker and schema folder from the starter package, then run:</p><pre><code>uv run validate_tei.py tei</code></pre><p>The checker validates against the supplied TEI Lite schema and checks document IDs, scan counts and local image paths. Compare metadata and text with the images separately. Bring one corrected error and one unresolved reading to the discussion.</p><p>Check Ira Malaniuk’s rows in _10 and _11 against their source images. Preserve each source’s statement about the announced cast.</p></section>
<section id="reference"><h2>Compare with the reference solutions</h2><p>Complete your own pass, then investigate differences from the reference. Resolve them against the facsimiles.</p><details><summary>Download TEI, metadata and edited full texts</summary><p>The reference contains seven complete TEI files and all 40 scans’ text. Metadata is accompanied by source evidence. The XML follows TEI Lite; the text retains its documented review status and remaining uncertain readings.</p><div class="actions">${download('m3gim-reference.zip', 'TEI, metadata & full texts · ZIP', 'button')}${download('metadata.csv', 'Metadata overview · CSV')}${download('metadata.json', 'Metadata & evidence · JSON')}${download('review-notes.md', 'Review notes & corrections')}</div><div class="table-scroll" role="region" aria-label="Reference metadata overview" tabindex="0"><table class="metadata-table"><thead><tr><th scope="col">Document</th><th scope="col">Source title</th><th scope="col">Languages</th><th scope="col">Date</th></tr></thead><tbody>${overview}</tbody></table></div><ul class="resources">${references}</ul><p class="small">The reference ZIP contains raw and edited transcriptions, TEI, metadata and checking files. Download the PNGs separately or use the complete bundle below.</p></details></section>
<section id="next-session" class="panel"><h2>Continue in Session 3</h2><p>Bring your complete working folder. The next session will reuse the TEI metadata and full texts, with the PNGs available to check the evidence.</p><pre><code>m3gim-work/
  tei/                 7 XML files
  png/                 40 PNG scans in 7 folders
  schema/
  validate_tei.py</code></pre><p>The reference bundle contains this folder structure with working relative image links.</p><div class="actions">${download('m3gim-next-session.zip', 'Reference TEI + all images · ZIP, ' + nextSize + ' MB', 'button')}${download('NEXT-SESSION.md', 'Read the corpus guide')}</div></section>
<section><h2>Slide text and documentation</h2><p>Five English slides with speaker notes, prepared for insertion after <em>Prompting Strategies: There Is No Prompt to Rule Them All</em>.</p><div class="actions">${download('slide-text.md', 'Download slide text · Markdown')}${download('manifest.json', 'Source and image manifest · JSON')}</div><p class="small">The XML structure follows ${link('https://tei-c.org/release/doc/tei-p5-exemplars/html/tei_lite.doc.html', 'TEI Lite')} and the TEI ${link('https://tei-c.org/release/doc/tei-p5-doc/en/html/ref-pb.html', 'page-beginning and image-link convention')}.</p></section>
<div class="session-nav">${link('../index.html#session-2', '← Session 2')}${link('../index.html#session-3', 'Continue to Session 3 →')}</div>`;
}
